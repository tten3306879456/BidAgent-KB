# 标书智能体知识库 — Python 依赖清单
# 默认后端(local_search)只需 Python 标准库，无需以下依赖

# === 默认安装(可选) ===
PyPDF2>=3.0              # PDF文件支持(本地检索/自动索引)
requests>=2.28           # 网页URL导入(ima后端)

# === ima 云端后端(可选) ===
cos-python-sdk-v5>=1.9   # 腾讯云COS上传(ima后端上传文件)

# === ChromaDB 高级后端(可选) ===
# 仅当 backend.type = "chromadb" 时需要
# 首次运行需下载嵌入模型(~90MB)
chromadb>=0.4            # 本地向量数据库
sentence-transformers>=2.2  # 嵌入模型

# === Word文件支持(可选) ===
# python-docx>=0.8       # Word文档读取
